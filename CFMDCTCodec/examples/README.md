# Examples

Place short, redistributable test audio files under `examples/wavs/` before
running inference examples from the main README.

Audio files are intentionally not included in this source snapshot. If you add
demo audio before publishing, make sure its license allows redistribution.

